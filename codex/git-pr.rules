# Route direct Git through PermissionRequest; PR commands are always allowed.
prefix_rule(pattern=[["git", "/usr/bin/git", "/bin/git"]], decision="prompt")
prefix_rule(pattern=["gh", "pr"], decision="allow")
