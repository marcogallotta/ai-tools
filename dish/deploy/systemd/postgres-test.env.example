# Owner-readable only (0600). Read by dish-postgres-test.service via
# `docker compose --env-file`.
DISH_POSTGRES_DB=dish_stage_a_test
DISH_POSTGRES_USER=dish
DISH_POSTGRES_PASSWORD=replace-with-test-password
DISH_POSTGRES_HOST_PORT=55432
