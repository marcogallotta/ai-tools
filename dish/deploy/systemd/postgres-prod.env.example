# Owner-readable only (0600). Production instance; do not commit populated secrets.
# Read by dish-postgres-prod.service via `docker compose --env-file`.
DISH_POSTGRES_DB=dish_stage_a_prod
DISH_POSTGRES_USER=dish
DISH_POSTGRES_PASSWORD=replace-with-prod-password
DISH_POSTGRES_HOST_PORT=55433
