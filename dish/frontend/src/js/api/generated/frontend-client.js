// Generated by tools/generate-client.mjs. Do not edit.
export const FRONTEND_OPERATION_IDS = [
  "frontendLogin",
  "frontendLogout",
  "getFrontendBoard",
  "getFrontendSectionTasks",
  "getFrontendSession",
  "getFrontendTaskDetail"
];

export class GeneratedFrontendClient {
  constructor(transport) {
    this.transport = transport;
  }

  frontendLogin(options = {}) {
    const path = "/frontend/login";
    return this.transport.request({ path, method: "POST", ...options });
  }

  frontendLogout(options = {}) {
    const path = "/frontend/logout";
    return this.transport.request({ path, method: "POST", ...options });
  }

  getFrontendBoard(options = {}) {
    const path = "/frontend/board";
    return this.transport.request({ path, method: "GET", ...options });
  }

  getFrontendSectionTasks(section_id, options = {}) {
    const path = "/frontend/sections/{section_id}/tasks".replace("{section_id}", encodeURIComponent(section_id));
    return this.transport.request({ path, method: "GET", ...options });
  }

  getFrontendSession(options = {}) {
    const path = "/frontend/session";
    return this.transport.request({ path, method: "GET", ...options });
  }

  getFrontendTaskDetail(task_id, options = {}) {
    const path = "/frontend/tasks/{task_id}".replace("{task_id}", encodeURIComponent(task_id));
    return this.transport.request({ path, method: "GET", ...options });
  }
}
