# Canonical repository-changing implementation/fix handoff

This repository file is the canonical shared source for Dish Implementation/fix handoffs. Every handoff must name the repository, Asana task GID, authorized branch, exact base ref and SHA, existing PR or `none`, and exact PR head when one exists. Matching Asana task identity on another branch or PR does not authorize that lineage.
